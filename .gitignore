# =====================================================================
# 本仓库只保留三类内容：
#   1) Core/ 下的源代码与头文件（含 Core/app 与 Core/sensors）
#   2) 工程配置：power_control.ioc、.mxproject，以及 EIDE 的构建清单
#      MDK-ARM/.eide/eide.yml（本工程以 EIDE 为主线，Keil 只借器件包
#      与编译器；uvprojx 备查保留，uvoptx 属本机调试参数，不进仓库）
#   3) 最终编译产物 hex（见 MDK-ARM/.gitignore 的放行规则）
# 其余一切与源代码无关的内容全部屏蔽。
# 克隆后若需编译：装 VS Code 的 EIDE 扩展并打开 MDK-ARM/.eide/eide.yml；
# Drivers 与 Middlewares 由 CubeMX / EIDE 重新准备即可。
# =====================================================================

# ---------- 第三方库：整体屏蔽（CubeMX 可重新生成）----------
/Drivers/
/Middlewares/

# ---------- 其它与源代码无关的目录与压缩包 ----------
/Documentation/
/*.pdf
/*.zip
/*.rar
/*.7z
/*.exe
/*.msi

# ---------- 操作系统 / 编辑器垃圾文件 ----------
Thumbs.db
Desktop.ini
ehthumbs.db
.DS_Store
*.lnk
*.bak
*.tmp
*.orig
*.rej
*.swp
*~

# ---------- 编译输出目录 ----------
# MDK-ARM/build 与 MDK-ARM/power_control 只保留文件夹壳（.gitkeep 占位），
# 详细规则见 MDK-ARM/.gitignore；其中 power_control.hex 已放行。
